import 'package:flutter/material.dart';

class ServiceProviderViewall extends StatelessWidget {
  const ServiceProviderViewall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
